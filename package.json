{
  "name": "nigeria-forest-monitor",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @nfm/web",
    "build:web": "npm run build --workspace @nfm/web",
    "lint:web": "npm run lint --workspace @nfm/web",
    "typecheck:web": "npm run typecheck --workspace @nfm/web",
    "check:web": "npm run check --workspace @nfm/web"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
