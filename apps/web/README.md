# Web

Next.js and MapLibre application shell for the private monitoring workspace.
The Phase 2 map deliberately uses no third-party basemap or live data source.

```powershell
npm install
npm run dev:web
```
